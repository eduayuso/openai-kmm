import SwiftUI
import Shared

struct Theme {

    // MARK: Colors
    struct Colors {

        static let primary = Color.blue
        static let shadow = Color.gray
        static let menuBackground = Color.white
    }
}
