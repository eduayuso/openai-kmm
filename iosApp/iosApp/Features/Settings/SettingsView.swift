import SwiftUI
import Resolver
import Shared

struct SettingsView: View {
    
    @InjectedObject var viewModel: SettingsModel
    
    var body: some View {
        
        Text("hola settings")
    }
}
